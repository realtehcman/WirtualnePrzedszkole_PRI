package com.example.wirtualneprzedszkole.service;
/*
import com.example.wirtualneprzedszkole.model.dao.FileData;
import com.example.wirtualneprzedszkole.repository.HashedFileRepo;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;

@Service
@RequiredArgsConstructor
public class HashedFIleService {
    private final HashedFileRepo hashedFileRepo;
    public FileData createHashedFile(String hashedFile) {
        HashedFileRepo.save(hashedFile);
    }
}*/
